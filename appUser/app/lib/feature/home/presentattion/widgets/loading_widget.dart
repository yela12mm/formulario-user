import 'package:flutter/material.dart';

class Load_Wid extends StatelessWidget{
  const Load_Wid({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: CircularProgressIndicator(),
    );
  }
}