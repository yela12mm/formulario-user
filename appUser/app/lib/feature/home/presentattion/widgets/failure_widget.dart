import 'package:flutter/material.dart';

class Fail_wid extends StatelessWidget{
  const Fail_wid({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child:Text("error")
    );
  }
}